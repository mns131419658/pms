# 首次設定指南

這是一套可以在自己電腦上執行的**專案管理系統**，適合裝修工程、設計、營造類的公司使用。
功能包含客戶與廠商資料、報價單、專案進度（甘特圖）、維修工單、應收應付帳款、
會計與員工薪資，以及台灣的營業稅 401 申報與扣繳彙總報表。

資料存在你自己的電腦裡，不會上傳到任何地方。

---

## 一、安裝與啟動

### 1. 先裝 Node.js

系統需要 Node.js 才能執行。到 <https://nodejs.org/> 下載 **LTS 版**安裝，
或在終端機執行：

```bash
winget install OpenJS.NodeJS.LTS
```

> 裝完請**重新開機或至少重開檔案總管**，否則系統可能還找不到它。

### 2. 啟動

把整個資料夾解壓縮到一個**路徑單純**的位置，建議 `C:\pms`（避免放在有中文或空白的深層路徑）。

雙擊 **`啟動系統.bat`**。第一次會自動安裝相依套件（需要網路，約 10 秒），
接著自動開啟瀏覽器到 <http://localhost:3000/>。

要停止系統，直接關閉那個黑色視窗。

---

## 二、第一次登入（重要）

| | |
|---|---|
| 員工代號 | `A0000` |
| 密碼 | `0000` |

**登入後請立刻點左下角「變更密碼」改掉。** 這組預設密碼是公開的，不改等於門沒鎖。

### 建立其他員工帳號

到左側「帳號管理」→「＋ 新增帳號」。每個帳號可以個別勾選八種權限：

系統管理者、客戶基本資料表、報價單、專案管理資料表、廠商基本資料表、
應付款項、應收款項、會計

沒勾到的功能，左側選單就不會出現。**勾了「系統管理者」等於擁有全部權限。**

> 建議至少建立**兩個**系統管理者帳號互為備援。密碼連續打錯 5 次帳號會被鎖住，
> 這時需要另一個管理者到「帳號管理」把狀態改回「正常」才能解鎖。

---

## 三、改成你自己公司的樣子

### 1. 改系統名稱

搜尋並取代這兩個檔案裡的「專案管理系統」字樣：

| 檔案 | 位置 |
|---|---|
| `public/index.html` | 第 6 行 `<title>`、第 14 行 `<h1>` |
| `public/app.html` | 第 6 行 `<title>`、第 13 行側邊欄名稱 |

### 2. 換 LOGO

直接把你的圖片覆蓋 **`public/img/logo.jpg`** 就好，檔名不要改。
如果你的圖檔是 PNG，請把檔名也改成 `logo.jpg`（或請工程師協助，同步修改
`public/index.html`、`public/app.html`、`public/js/app.js` 裡的 `logo.jpg` 副檔名）。

這一個檔案會同時套用到 6 個地方：登入畫面、側邊欄、瀏覽器分頁圖示（兩處），
以及報價單、請款單、付款憑証三種列印表頭。

LOGO 圖片本身要用**白色底圖**（不要透明背景），長方形或正方形都可以，
畫面會依圖片比例自動縮放、不會裁切成圓形。換好後在瀏覽器按 `Ctrl + F5` 強制重新整理。

### 3. 換首頁流程圖 ⚠️ 這裡有陷阱

首頁的流程圖 **`public/img/home.png`**（1315×1196）上有 **6 個看不見的可點擊區域**，
分別蓋在圖上那六張卡片的位置，點下去就會跳到對應功能。

這些區域是**寫死的百分比座標**，並不是從圖片自動偵測出來的。
**所以換一張版面不同的圖，六個點擊區會全部錯位**——例如點「客戶資料」卻跳到「廠商資料」。

目前六個熱區與圖上卡片的對應如下：

| 圖上卡片 | 位置 | 點擊後前往 | 座標（左 / 上 / 寬 / 高，單位為百分比） |
|---|---|---|---|
| 客戶資料 | 左上 | 客戶資料 | 8.2 / 11.5 / 36.5 / 16.8 |
| 廠商資料 | 右上 | 廠商資料 | 55.0 / 11.5 / 36.0 / 16.8 |
| 報價資料 | 中間 | 報價資料 | 32.0 / 34.0 / 35.5 / 14.5 |
| 專案管理 | 中央大卡 | 專案管理 | 23.8 / 51.5 / 52.5 / 18.2 |
| 應收帳款 | 左下 | 應收款項 | 8.2 / 75.5 / 33.0 / 16.0 |
| 應付帳款 | 右下 | 應付款項 | 57.0 / 75.5 / 32.0 / 16.0 |

如果你不打算換圖，這一節可以直接跳過，維持現狀即可正常運作。
要換的話有兩個選擇：

**做法 A（簡單）**：設計新圖時，把六張卡片畫在**跟上表相同的相對位置**。
圖片尺寸可以不一樣（因為座標是百分比），只要版面比例相同即可。

**做法 B（自由）**：想怎麼設計都行，但要同步修改座標。
打開 `public/js/app.js`，找到第 **2773** 行附近的 `HOME_SPOTS`：

```js
const HOME_SPOTS = [
  { key: 'customer',   l: 8.2,  t: 11.5, w: 36.5, h: 16.8 },   // 客戶資料
  { key: 'vendor',     l: 55.0, t: 11.5, w: 36.0, h: 16.8 },   // 廠商資料
  ...
];
```

`l` 是左邊界、`t` 是上邊界、`w` 是寬、`h` 是高，全部都是**佔整張圖的百分比**。
算法：`左邊界百分比 = 方塊左緣像素 ÷ 圖片總寬度 × 100`。

改完存檔，重新整理瀏覽器即可，不用重啟伺服器。

> 想確認熱區對不對，把滑鼠移到圖上，游標變成手指形狀的地方就是可點擊區。

### 4. 換配色

所有顏色集中在 `public/css/style.css` 最上面的 `:root` 區塊：

```css
:root {
  --bg: #f5f7f0;            /* 背景 */
  --logo: #98c810;          /* 品牌主色 */
  --primary: #6b9a10;       /* 按鈕、連結 */
  --primary-dark: #567f0c;  /* 滑鼠移過去 */
  --accent: #98c810;        /* 高亮 */
  --sidebar-bg: #26340c;    /* 側邊欄底色 */
  ...
}
```

改這幾個值就會整套跟著變。

另外三種列印表單（報價單、請款單、付款憑証）的表頭橫線顏色是**另外寫死的**，
在 `public/js/app.js` 搜尋 `#98c810`，共 3 處，一併改成你的主色。

### 5. 客製下拉選單（不用改程式）

這是最常需要調整的部分，**完全不需要碰程式碼**。

用管理者登入後到左側「**項目維護**」，可以自行增刪修改這八組清單：

- 廠商－營業項目、結帳方式
- 應收款項－項目、收款方式
- 應付款項－項目、付款方式、費用類別
- 專案管理－專案狀態

> 「專案狀態」這組還有個額外用途：新增專案時如果選擇「分幾個階段」，
> 系統會依這份清單的順序自動命名各階段並平均分配預定完成日。
> 建議照你們實際的施工順序排列。

---

## 四、備份

整個 **`data`** 資料夾就是全部的資料：

| 路徑 | 內容 |
|---|---|
| `data/db.json` | 所有資料（客戶、廠商、專案、報價、帳款、帳號…） |
| `data/files/` | 專案結案附件的實體檔案 |
| `data/session-secret.txt` | 登入用的簽章金鑰，首次啟動自動產生 |

**備份就是複製整個 `data` 資料夾**，還原就是複製回去（複製前請先關閉系統）。
建議定期複製到隨身碟或雲端硬碟。

系統內另有「**系統還原**」功能（限管理者），可以從備份檔還原；
它在執行前會自動先下載一份目前資料的備份到你的電腦，避免還錯檔救不回來。

---

## 五、常見問題

**Q：雙擊後視窗閃一下就不見了**
八成是沒裝 Node.js。視窗現在遇到任何錯誤都會停住等你按鍵，如果真的瞬間消失，
請改用終端機進到資料夾執行 `npm start`，錯誤訊息就會留在畫面上。

**Q：顯示「連接埠 3000 已經有程式在使用」**
系統已經在跑了，直接開 <http://localhost:3000/> 即可。
如果要改用別的埠號，在終端機執行 `set PORT=3001` 後再 `npm start`。

**Q：忘記管理者密碼、或帳號被鎖住**
最簡單的方法是請另一位管理者到「帳號管理」重設。
如果所有管理者都進不去，先**關閉系統**，然後在資料夾裡開終端機執行：

```bash
node -e "const{hashPassword}=require('./src/util');const fs=require('fs');const f='./data/db.json';const d=JSON.parse(fs.readFileSync(f,'utf8'));const a=d.accounts[0];a.password=hashPassword('0000');a.status='正常';a.fail_count=0;fs.writeFileSync(f,JSON.stringify(d,null,2));console.log('已將',a.emp_id,'的密碼重設為 0000');"
```

重新啟動後用 `0000` 登入，然後立刻改密碼。

**Q：AI 發票辨識按了沒反應**
這個功能會呼叫外部 AI 服務，需要自備金鑰。設定環境變數 `ANTHROPIC_API_KEY`
後重新啟動即可使用。沒設定時會顯示提示訊息，其他欄位仍可手動填寫，不影響使用。

**Q：可以讓同事從別台電腦連進來嗎？**
可以，把防火牆的 3000 埠開放，同事用 `http://你的IP:3000/` 連線。
但這套系統沒有加密連線（HTTPS）也沒有做對外的安全防護，
**僅建議在公司內部網路使用，不要直接暴露到網際網路上。**

---

## 六、要把系統放上網路之前，先看這一段

如果你打算把系統放到 GitHub、部署到雲端讓同事連線，有兩個檔案務必留意：

**`.gitignore`（已內附，請不要刪除）**

它負責擋住不該上傳的東西，包含：

| 被擋住的 | 為什麼 |
|---|---|
| `data/` | 你的客戶姓名、手機、報價金額、應收應付、員工薪資 |
| `.env` | 資料庫連線字串與 Google 金鑰，等於帳號密碼 |
| `get-token.js` | 取得授權的一次性工具 |

**上傳到 GitHub 的東西，即使事後刪除仍會留在提交歷史裡**，很難清乾淨。
所以請務必在第一次上傳「之前」確認 `.gitignore` 存在且生效。

**`.env.example`（設定檔範本）**

要連資料庫或雲端硬碟時，把它複製一份改名成 `.env`，再把值填進去。
系統啟動時會自動讀取（Node 20.12 以上內建支援，不需安裝套件）；
沒有 `.env` 就自動改用本機資料夾存放，不影響單機使用。

---

## 七、給工程師的補充

技術細節、資料表結構、商業邏輯的注意事項，請看 `CLAUDE.md` 與 `README.md`。

簡單說：Node.js + Express，資料存單一 JSON 檔，
除 `express` 與 `express-session` 外沒有任何相依套件，也沒有原生模組，
整個資料夾可以直接複製到別台電腦執行。
